import pygame
pygame.init()

screen = pygame.display.set_mode((480,700))

hero = pygame.image.load("./GameImages/me1.png")
screen.blit(hero, (0,0))
pygame.display.update()
while True:
    pass
pygame.quit()
